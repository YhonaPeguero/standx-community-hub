/**
 * Hero canvas — neutralized in the trading-deck redesign.
 *
 * The three.js scene was replaced by a static grid mesh in the hero JSX.
 * Component retained as a no-op so any lingering imports still compile.
 */
export default function HeroCanvas() {
  return null;
}
