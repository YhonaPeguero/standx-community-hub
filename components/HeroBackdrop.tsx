/**
 * Hero backdrop — neutralized in the trading-deck redesign.
 *
 * Previously rendered a muted looping video behind the hero. The new visual
 * direction (Hyperliquid-style monochrome) does not use video. Component is
 * kept so existing imports compile; it now renders nothing.
 */
export default function HeroBackdrop() {
  return null;
}
